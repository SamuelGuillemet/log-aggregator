{
  "name": "log-aggregator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "concurrently \"pnpm --filter @log-aggregator/server dev\" \"pnpm --filter @log-aggregator/client dev\"",
    "lint": "biome check .",
    "format": "biome format --write .",
    "check": "biome check --write .",
    "package:local": "node scripts/package-local-runner.mjs",
    "doctor": "npx react-doctor@latest",
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.2",
    "concurrently": "^10.0.4",
    "react-doctor": "^0.8.1",
    "typescript": "^7.0.2"
  }
}
